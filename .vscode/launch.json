{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "SUMO RL Debug (Current File)",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "cwd": "${fileDirname}",
            "justMyCode": false
        }
    ]
}
